#pragma once
/*#include "Command.h"
#include <list>
#include <unordered_map>
#include <SDL.h>

class InputSystem
{
public:
	InputSystem();
	~InputSystem();

	Command* handleInput(Uint32 input);

private:

	Command * createCommand(std::string str);

	std::list<Command*> commands;
	std::unordered_map<Uint32, std::string> mappings;
	// quueue/vecteur? de commands pour undo/redo
};
*/
